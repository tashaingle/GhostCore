import {NextResponse} from "next/server";import {organisationApiContext} from "@/lib/organisations/api-context";import {createDemoEvents} from "@/lib/events/demo";import {createEvent} from "@/lib/events/create-event";import {hasPermission,type OrganisationRole} from "@/lib/auth/permissions";
export async function POST(){const ctx=await organisationApiContext();if(!ctx)return NextResponse.json({error:{code:"unauthorized",message:"Authentication and an active organisation are required."}},{status:401});if(!hasPermission(ctx.membership.role as OrganisationRole,"event.create"))return NextResponse.json({error:{code:"forbidden",message:"This role is read-only."}},{status:403});let created=0;for(const event of createDemoEvents(ctx.organisationId)){const result=await createEvent(ctx.supabase,ctx.user.id,event);if(result.ok&&!result.duplicate)created++}return NextResponse.json({data:{created,organisationId:ctx.organisationId}})}
