import{NextResponse}from"next/server";import{cookies}from"next/headers";import{createClient}from"@/lib/supabase/server";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{exchangeCode,stateMatches}from"@/lib/integrations/google-search-console/oauth";import{encryptToken}from"@/lib/security/token-crypto";import{SearchConsoleClient}from"@/lib/integrations/google-search-console/client";import type{Json}from"@/types/database";
const back=(url:URL,kind:"error"|"success",message:string,path="/app/integrations")=>NextResponse.redirect(new URL(`${path}?${kind}=${encodeURIComponent(message)}`,url));
export async function GET(request:Request){const url=new URL(request.url),store=await cookies(),expected=store.get("ghost_gsc_state")?.value,verifier=store.get("ghost_gsc_verifier")?.value,organisationId=store.get("ghost_gsc_org")?.value;store.delete("ghost_gsc_state");store.delete("ghost_gsc_verifier");store.delete("ghost_gsc_org");if(url.searchParams.get("error"))return back(url,"error","Google Search Console authorization was denied.");const code=url.searchParams.get("code");if(!stateMatches(expected,url.searchParams.get("state"))||!code||!verifier||!organisationId)return back(url,"error","Search Console authorization expired or failed validation.");try{const supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Your Ghost session expired.");const{data:member}=await supabase.from("organisation_members").select("role").eq("organisation_id",organisationId).eq("user_id",user.id).eq("status","active").maybeSingle();if(!member||!hasPermission(member.role as OrganisationRole,"integration.manage"))throw new Error("You no longer have permission to connect Search Console here.");const tokens=await exchangeCode(code,verifier),profileResponse=await fetch("https://openidconnect.googleapis.com/v1/userinfo",{headers:{Authorization:`Bearer ${tokens.access_token}`},signal:AbortSignal.timeout(15_000)}),profile=profileResponse.ok?await profileResponse.json()as{sub?:string;email?:string}:{},client=new SearchConsoleClient({accessToken:tokens.access_token!,refreshToken:tokens.refresh_token,expiresAt:new Date(Date.now()+(tokens.expires_in??3600)*1000).toISOString()}),properties=await client.properties();if(!profile.sub)throw new Error("Google account identity could not be verified.");const{data:existing}=await supabase.from("integrations").select("id,refresh_token_encrypted,settings").eq("organisation_id",organisationId).eq("provider","google_search_console").eq("provider_account_id",profile.sub).maybeSingle();if(!tokens.refresh_token&&!existing?.refresh_token_encrypted)throw new Error("Google returned no refresh token. Remove Ghost access from Google and reconnect.");const old=existing?.settings&&typeof existing.settings==="object"&&!Array.isArray(existing.settings)?existing.settings as Record<string,unknown>:{},oldProps=Array.isArray(old.properties)?old.properties as{siteUrl:string;selected?:boolean}[]:[],settings={...old,accountEmail:profile.email??null,configurationStatus:"property_required",properties:properties.map(p=>({...p,selected:Boolean(oldProps.find(x=>x.siteUrl===p.siteUrl)?.selected)})),thresholdPercent:Number(old.thresholdPercent??25),minimumClicks:Number(old.minimumClicks??10),initialSyncComplete:Boolean(old.initialSyncComplete)} as Json,values={provider_account_id:profile.sub,provider_account_name:profile.email??"Google account",status:"connected",access_token_encrypted:encryptToken(tokens.access_token!),refresh_token_encrypted:tokens.refresh_token?encryptToken(tokens.refresh_token):existing?.refresh_token_encrypted??null,token_expires_at:new Date(Date.now()+(tokens.expires_in??3600)*1000).toISOString(),settings,last_sync_status:"connected",last_sync_error:null};const result=existing?await supabase.from("integrations").update(values).eq("id",existing.id).eq("organisation_id",organisationId):await supabase.from("integrations").insert({organisation_id:organisationId,provider:"google_search_console",...values});if(result.error)throw new Error("Search Console authorization succeeded, but the integration could not be saved.");return back(url,"success",`Search Console connected as ${profile.email??"Google account"}.`,"/app/integrations/google-search-console/properties")}catch(error){return back(url,"error",error instanceof Error?error.message:"Search Console connection failed.")}}
