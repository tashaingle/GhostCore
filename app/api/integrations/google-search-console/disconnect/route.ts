import{organisationApiContext}from"@/lib/organisations/api-context";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";
export async function POST(request:Request){const ctx=await organisationApiContext();if(!ctx)return Response.json({error:"Authentication required."},{status:401});if(!hasPermission(ctx.membership.role as OrganisationRole,"integration.manage"))return Response.json({error:"Owner or admin access is required."},{status:403});let body:{integrationId?:string};try{body=await request.json()}catch{return Response.json({error:"Invalid JSON."},{status:400})}const{error}=await ctx.supabase.from("integrations").update({status:"disconnected",access_token_encrypted:null,refresh_token_encrypted:null,token_expires_at:null,last_sync_status:"disconnected",last_sync_error:null}).eq("id",body.integrationId??"").eq("organisation_id",ctx.organisationId).eq("provider","google_search_console");return error?Response.json({error:"Disconnect failed."},{status:500}):Response.json({ok:true})}
