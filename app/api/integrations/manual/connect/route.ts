import{NextResponse}from"next/server";import{organisationApiContext}from"@/lib/organisations/api-context";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{ensureManualIntegration}from"@/lib/integrations/manual/service";export async function GET(request:Request){const ctx=await organisationApiContext();if(!ctx)return NextResponse.redirect(new URL("/login",request.url));if(!hasPermission(ctx.membership.role as OrganisationRole,"integration.manage"))return NextResponse.redirect(new URL("/app/integrations?error=Owner%20or%20admin%20access%20is%20required.",request.url));try{await ensureManualIntegration(ctx.supabase,ctx.organisationId);return NextResponse.redirect(new URL("/app/integrations/manual?success=Manual%20provider%20enabled.",request.url))}catch(error){return NextResponse.redirect(new URL(`/app/integrations?error=${encodeURIComponent(error instanceof Error?error.message:"Manual provider could not be enabled.")}`,request.url))}}
