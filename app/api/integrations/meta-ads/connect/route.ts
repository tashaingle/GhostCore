import{NextResponse}from"next/server";import{cookies}from"next/headers";import{organisationApiContext}from"@/lib/organisations/api-context";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{authorisationUrl,newState}from"@/lib/integrations/meta-ads/oauth";import{metaEnv}from"@/lib/integrations/meta-ads/config";export async function GET(request:Request){const ctx=await organisationApiContext();if(!ctx)return NextResponse.redirect(new URL("/login",request.url));if(!hasPermission(ctx.membership.role as OrganisationRole,"integration.manage"))return NextResponse.redirect(new URL("/app/integrations?error=Owner%20or%20admin%20access%20is%20required.",request.url));try{const origin=new URL(request.url).origin,expected=new URL("/api/integrations/meta-ads/callback",origin).toString();if(metaEnv().redirectUri!==expected)throw new Error(`Meta redirect URI must be exactly ${expected}`);const state=newState(),payload=JSON.stringify({state,userId:ctx.user.id,organisationId:ctx.organisationId,provider:"meta_ads",returnTo:"/app/integrations/meta-ads/settings",createdAt:Date.now()}),store=await cookies();store.set("ghost_meta_oauth",payload,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/api/integrations/meta-ads",maxAge:600});return NextResponse.redirect(authorisationUrl(state))}catch(error){return NextResponse.redirect(new URL(`/app/integrations?error=${encodeURIComponent(error instanceof Error?error.message:"Meta Ads connection could not be started.")}`,request.url))}}
