import{NextResponse}from"next/server";import{cookies}from"next/headers";import{organisationApiContext}from"@/lib/organisations/api-context";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{newSlackState,slackAuthorisationUrl}from"@/lib/integrations/slack/oauth";import{slackEnv}from"@/lib/integrations/slack/config";export async function GET(request:Request){const ctx=await organisationApiContext();if(!ctx)return NextResponse.redirect(new URL("/login",request.url));if(!hasPermission(ctx.membership.role as OrganisationRole,"integration.manage"))return NextResponse.redirect(new URL("/app/integrations?error=Owner%20or%20admin%20access%20is%20required.",request.url));try{const expected=new URL("/api/integrations/slack/callback",new URL(request.url).origin).toString();if(slackEnv().redirectUri!==expected)throw new Error(`Slack redirect URI must be exactly ${expected}`);const state=newSlackState(),payload=JSON.stringify({state,userId:ctx.user.id,organisationId:ctx.organisationId,provider:"slack",returnTo:"/app/integrations/slack/settings",createdAt:Date.now()}),store=await cookies();store.set("ghost_slack_oauth",payload,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/api/integrations/slack",maxAge:600});return NextResponse.redirect(slackAuthorisationUrl(state))}catch(error){return NextResponse.redirect(new URL(`/app/integrations?error=${encodeURIComponent(error instanceof Error?error.message:"Slack connection could not be started.")}`,request.url))}}
