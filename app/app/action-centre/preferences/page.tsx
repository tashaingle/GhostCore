import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{Notice}from"@/components/notice";import{saveNotificationPreference}from"@/app/notification-actions";import{notificationCategories}from"@/lib/notifications/types";
export default async function Preferences({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams,{data:rows}=await ctx.supabase.from("notification_preferences").select("*").eq("organisation_id",ctx.organisation.id),canOrg=hasPermission(ctx.membership.role as OrganisationRole,"notifications.rules.manage"),user=rows?.find(x=>x.user_id===ctx.user.id&&!x.category),org=rows?.find(x=>!x.user_id&&!x.category);const form=(scope:"user"|"organisation",current:typeof user)=><form action={saveNotificationPreference} className="card grid gap-3 md:grid-cols-2"><input type="hidden" name="scope" value={scope}/><h3 className="font-semibold md:col-span-2">{scope==="user"?"My preferences":"Organisation defaults"}</h3><label>Category<select className="field ml-2" name="category"><option value="">All categories</option>{notificationCategories.map(x=><option key={x}>{x}</option>)}</select></label><label>Minimum severity<select className="field ml-2" name="minimumSeverity" defaultValue={current?.minimum_severity??"info"}>{["info","warning","critical"].map(x=><option key={x}>{x}</option>)}</select></label>{[["inApp","In-app enabled",current?.in_app_enabled??true],["assignment","Assignment-related",current?.assignment_enabled??true],["email","Email preference (delivery deferred)",current?.email_enabled??false],["webhook","Webhook preference (delivery deferred)",current?.webhook_enabled??false]].map(([name,label,checked])=><label className="flex gap-2" key={String(name)}><input name={String(name)} type="checkbox" defaultChecked={Boolean(checked)}/>{label}</label>)}<label>Digest<select className="field ml-2" name="digestMode" defaultValue={current?.digest_mode??"immediate"}>{["immediate","daily","weekly","off"].map(x=><option key={x}>{x}</option>)}</select></label><button className="button md:col-span-2">Save preferences</button></form>;return <section className="space-y-6"><Link className="text-sm text-zinc-500" href="/app/action-centre">← Action Centre</Link><div><h2 className="text-2xl font-bold">Notification preferences</h2><p className="text-zinc-600">Precedence: mandatory rule, user override, organisation default, system default.</p><p className="text-sm text-zinc-500">In-app delivery works now. Email and webhook settings are stored for future delivery and do not send messages in this phase.</p></div><Notice searchParams={p}/>{form("user",user)}{canOrg&&form("organisation",org)}</section>}
