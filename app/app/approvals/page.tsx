import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{Notice}from"@/components/notice";
export default async function Approvals({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams,status=typeof p.status==="string"?p.status:"pending",mine=p.mine==="1";let query=ctx.supabase.from("workflow_approvals").select("*").eq("organisation_id",ctx.organisation.id).order("created_at",{ascending:false}).limit(200);if(status)query=query.eq("status",status);if(mine)query=query.eq("approver_user_id",ctx.user.id);const{data:rows}=await query,now=new Date();return <section className="space-y-6"><div><h2 className="text-2xl font-bold">Approvals</h2><p className="text-zinc-600">Human decisions requested by deterministic workflows.</p></div><Notice searchParams={p}/><div className="grid gap-3 md:grid-cols-5">{[["Pending",rows?.filter(x=>x.status==="pending").length??0],["Approved",rows?.filter(x=>x.status==="approved").length??0],["Rejected",rows?.filter(x=>x.status==="rejected").length??0],["Overdue",rows?.filter(x=>x.status==="pending"&&x.due_at&&new Date(x.due_at)<now).length??0],["Assigned to me",rows?.filter(x=>x.approver_user_id===ctx.user.id).length??0]].map(([k,v])=><div className="card" key={k}><p className="text-sm">{k}</p><p className="text-xl font-bold">{v}</p></div>)}</div><form className="card flex gap-2"><select className="field" name="status" defaultValue={status}><option value="">All statuses</option>{["pending","approved","rejected","expired","cancelled"].map(x=><option key={x}>{x}</option>)}</select><label className="flex items-center gap-2"><input type="checkbox" name="mine" value="1" defaultChecked={mine}/>Assigned to me</label><button className="button">Filter</button></form><div className="grid gap-3">{!rows?.length?<div className="card text-zinc-500">No approvals match these filters.</div>:rows.map(x=><Link className="card block" href={`/app/approvals/${x.id}`} key={x.id}><div className="flex justify-between"><strong>Workflow approval</strong><span className="capitalize">{x.status}</span></div><p className="text-sm">Run {x.run_id} · {x.approver_role??x.approver_user_id??"Unassigned"}</p><p className="text-xs text-zinc-500">Due {x.due_at?new Date(x.due_at).toLocaleString():"not set"}</p></Link>)}</div></section>}
