import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{WorkForm}from"@/components/work-form";import{Notice}from"@/components/notice";
export default async function NewCase({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams,{data:members}=await ctx.supabase.from("organisation_members").select("user_id").eq("organisation_id",ctx.organisation.id).eq("status","active");return <section className="mx-auto max-w-4xl space-y-6"><Link href="/app/cases">← Cases</Link><div><h2 className="text-2xl font-bold">Create case</h2><p className="text-zinc-600">Open a durable operational record backed by explicit evidence.</p></div><Notice searchParams={p}/><WorkForm kind="case" members={members??[]} sourceType={typeof p.sourceType==="string"?p.sourceType:undefined} sourceId={typeof p.sourceId==="string"?p.sourceId:undefined}/></section>}
