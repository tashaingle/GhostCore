import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{requireOrganisationAdmin}from"@/lib/auth/organisation-admin";import{saveSearchConsoleProperties}from"@/app/google-search-console-actions";
export default async function SearchConsoleProperties(){const ctx=await getActiveOrganisation();if(!ctx)return null;requireOrganisationAdmin(ctx.membership.role);const{data:integrations}=await ctx.supabase.from("integrations").select("id,provider_account_name,settings,status").eq("organisation_id",ctx.organisation.id).eq("provider","google_search_console").order("created_at");return <section className="mx-auto max-w-3xl space-y-6"><div><h2 className="text-2xl font-bold">Search Console properties</h2><p className="text-zinc-600">Select up to ten properties. Domain and URL-prefix properties remain scoped to this organisation.</p></div>{!integrations?.length?<div className="card"><p>Connect Search Console before selecting properties.</p><Link className="button mt-3" href="/api/integrations/google-search-console/connect">Connect Search Console</Link></div>:integrations.map(integration=>{const settings=integration.settings&&typeof integration.settings==="object"&&!Array.isArray(integration.settings)?integration.settings as Record<string,unknown>:{},properties=Array.isArray(settings.properties)?settings.properties as{siteUrl:string;permissionLevel:string;type:string;selected?:boolean}[]:[];return <form action={saveSearchConsoleProperties} className="space-y-3" key={integration.id}><input type="hidden" name="integrationId" value={integration.id}/><h3 className="font-semibold">Connected as {integration.provider_account_name}</h3>{properties.map(property=><label className="card flex gap-3" key={property.siteUrl}><input type="checkbox" name="property" value={property.siteUrl} defaultChecked={property.selected}/><span><strong>{property.siteUrl}</strong><span className="block text-sm text-zinc-500">{property.type.replace("_"," ")} · {property.permissionLevel}</span></span></label>)}<div className="card grid gap-3 sm:grid-cols-2"><label>Change threshold %<input className="input mt-1" type="number" min="5" max="100" name="thresholdPercent" defaultValue={Number(settings.thresholdPercent??25)}/></label><label>Minimum clicks<input className="input mt-1" type="number" min="1" max="1000" name="minimumClicks" defaultValue={Number(settings.minimumClicks??10)}/></label></div><button className="button">Save selected properties</button></form>})}</section>}
