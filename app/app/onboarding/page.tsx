import {createWorkspace} from "@/app/organisation-actions";import {Notice} from "@/components/notice";import {getActiveOrganisation} from "@/lib/organisations/active";import {redirect} from "next/navigation";
export default async function Onboarding({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){if(await getActiveOrganisation(true))redirect("/app");return <WorkspaceForm params={await searchParams} returnPath="/app/onboarding" title="Create your first organisation"/>}
function WorkspaceForm({params,returnPath,title}:{params:Record<string,string|string[]|undefined>;returnPath:string;title:string}){return <section className="mx-auto max-w-lg space-y-5"><div><h2 className="text-2xl font-bold">{title}</h2><p className="text-zinc-600">Each organisation is a separate, secure workspace.</p></div><Notice searchParams={params}/><form action={createWorkspace} className="card space-y-4"><input type="hidden" name="returnPath" value={returnPath}/><label className="label">Organisation name<input className="field" name="name" required minLength={2} maxLength={100}/></label><label className="label">Logo URL (optional)<input className="field" name="logoUrl" type="url"/></label><label className="label">Website (optional)<input className="field" name="website" type="url"/></label><label className="label">Industry (optional)<input className="field" name="industry" maxLength={100}/></label><button className="button">Create organisation</button></form></section>}
export {WorkspaceForm};
