import{getActiveOrganisation}from"@/lib/organisations/active";import{savedViewAction}from"@/app/work-actions";import{Notice}from"@/components/notice";
export default async function WorkViews({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams,{data}=await ctx.supabase.from("work_saved_views").select("*").eq("organisation_id",ctx.organisation.id).or(`user_id.eq.${ctx.user.id},is_shared.eq.true`).order("is_default",{ascending:false}).order("name");return <section className="space-y-6"><div><h2 className="text-2xl font-bold">Saved work views</h2><p className="text-zinc-600">Validated filters and deterministic sort definitions - never arbitrary SQL.</p></div><Notice searchParams={p}/><form action={savedViewAction} className="card grid gap-3 md:grid-cols-2"><label>Name<input className="field mt-1 w-full" name="name" required maxLength={100}/></label><label>View type<select className="field mt-1 w-full" name="viewType"><option value="unified">Unified work</option><option value="task">Tasks</option><option value="case">Cases</option></select></label><label className="md:col-span-2">Description<input className="field mt-1 w-full" name="description" maxLength={500}/></label><label>Validated filters (JSON array)<textarea className="field mt-1 min-h-24 w-full font-mono text-xs" name="filters" defaultValue="[]"/></label><label>Validated sort (JSON array)<textarea className="field mt-1 min-h-24 w-full font-mono text-xs" name="sort" defaultValue={'[{"field":"due_at","direction":"asc"}]'}/></label><label>Visible columns<input className="field mt-1 w-full" name="columns" defaultValue="title,status,priority,due_at"/></label><div className="flex gap-4"><label><input name="isDefault" type="checkbox"/> Default</label><label><input name="isShared" type="checkbox"/> Shared</label></div><button className="button md:col-span-2">Save view</button></form><div className="grid gap-4 md:grid-cols-2">{data?.map(x=><article className="card" key={x.id}><h3 className="font-semibold">{x.name}</h3><p>{x.description}</p><p className="mt-2 text-xs text-zinc-500">{x.view_type} · {x.is_shared?"Shared":"Personal"} {x.is_default?"· Default":""}</p><pre className="mt-2 overflow-auto text-xs">{JSON.stringify(x.filters_json,null,2)}</pre></article>)}</div></section>}
