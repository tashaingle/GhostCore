import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{workflowTriggers}from"@/lib/workflows/types";import{WorkflowBuilder}from"@/components/workflow-builder";import{saveWorkflow}from"@/app/workflow-actions";import{Notice}from"@/components/notice";
export default async function NewWorkflow({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams;if(!hasPermission(ctx.membership.role as OrganisationRole,"workflows.manage"))return <div className="card">You do not have permission to create workflows.</div>;return <section className="space-y-6"><Link href="/app/workflows">← Workflows</Link><div><h2 className="text-2xl font-bold">Workflow builder</h2><p className="text-zinc-600">Drag steps to reorder. Every save creates an immutable version.</p></div><Notice searchParams={p}/><form action={saveWorkflow} className="space-y-4"><div className="card grid gap-3 md:grid-cols-2"><label>Name<input className="field mt-1 w-full" name="name" required/></label><label>Trigger<select className="field mt-1 w-full" name="triggerType">{workflowTriggers.map(x=><option key={x}>{x}</option>)}</select></label><label className="md:col-span-2">Description<textarea className="field mt-1 w-full" name="description"/></label></div><WorkflowBuilder/><button className="button">Save workflow version 1</button></form></section>}
