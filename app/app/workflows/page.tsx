import Link from"next/link";import{getActiveOrganisation}from"@/lib/organisations/active";import{hasPermission,type OrganisationRole}from"@/lib/auth/permissions";import{Notice}from"@/components/notice";import{workflowTemplates}from"@/lib/workflows/templates";import{createFromTemplate}from"@/app/workflow-actions";
export default async function Workflows({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const ctx=await getActiveOrganisation(),p=await searchParams,q=typeof p.q==="string"?p.q.slice(0,100):"",status=typeof p.status==="string"?p.status:"";let query=ctx.supabase.from("workflow_definitions").select("*").eq("organisation_id",ctx.organisation.id).order("updated_at",{ascending:false}).limit(200);if(status)query=query.eq("status",status);if(q){const safe=q.replace(/[%(),]/g,"");query=query.or(`name.ilike.%${safe}%,description.ilike.%${safe}%,trigger_type.ilike.%${safe}%`)}const{data:rows}=await query,canManage=hasPermission(ctx.membership.role as OrganisationRole,"workflows.manage");return <section className="space-y-6"><div className="flex justify-between"><div><h2 className="text-2xl font-bold">Workflows</h2><p className="text-zinc-600">Versioned deterministic operations and human approvals.</p></div>{canManage&&<Link className="button" href="/app/workflows/new">Create workflow</Link>}</div><Notice searchParams={p}/><form className="card flex gap-2"><input className="field flex-1" name="q" defaultValue={q} placeholder="Search workflows"/><select className="field" name="status" defaultValue={status}><option value="">All statuses</option>{["draft","active","disabled","archived"].map(x=><option key={x}>{x}</option>)}</select><button className="button">Filter</button></form><div className="grid gap-3 lg:grid-cols-2">{!rows?.length?<div className="card text-zinc-500">No workflows have been created.</div>:rows.map(x=><Link className="card block" href={`/app/workflows/${x.id}`} key={x.id}><div className="flex justify-between"><strong>{x.name}</strong><span className="capitalize">{x.status}</span></div><p>{x.description}</p><p className="mt-2 text-xs text-zinc-500">{x.trigger_type} · version {x.current_version} · {x.enabled?"Enabled":"Disabled"}</p></Link>)}</div>{canManage&&<div><h3 className="mb-3 text-xl font-semibold">Templates</h3><div className="grid gap-3 lg:grid-cols-2">{workflowTemplates.map(t=><form action={createFromTemplate} className="card" key={t.key}><input type="hidden" name="templateKey" value={t.key}/><strong>{t.name}</strong><p className="text-sm">{t.description}</p><button className="button button-secondary mt-3">Use template</button></form>)}</div></div>}</section>}
