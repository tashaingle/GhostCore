"use server";import{redirect}from"next/navigation";import{getActiveOrganisation}from"@/lib/organisations/active";import{requireOrganisationAdmin}from"@/lib/auth/organisation-admin";import type{Json}from"@/types/database";export async function saveMetaAccounts(form:FormData){const ctx=await getActiveOrganisation();if(!ctx)redirect("/login");requireOrganisationAdmin(ctx.membership.role);const integrationId=String(form.get("integrationId")||""),selected=new Set(form.getAll("account").map(String).slice(0,10)),{data:item}=await ctx.supabase.from("integrations").select("settings").eq("id",integrationId).eq("organisation_id",ctx.organisation.id).eq("provider","meta_ads").maybeSingle();if(!item)redirect("/app/integrations?error=Meta%20Ads%20integration%20not%20found.");const settings=item.settings as Record<string,Json>,accounts=Array.isArray(settings.accounts)?settings.accounts.map(value=>{const a=value as Record<string,Json>;return{...a,selected:selected.has(String(a.accountId))}}):[];await ctx.supabase.from("integrations").update({settings:{...settings,accounts,configurationStatus:selected.size?"ready":"property_required"}}).eq("id",integrationId).eq("organisation_id",ctx.organisation.id);await ctx.supabase.from("integration_logs").insert({organisation_id:ctx.organisation.id,integration_id:integrationId,provider:"meta_ads",status:"finished",records_received:accounts.length,events_imported:0,events_skipped:0,error_count:0,metadata:{operation:"account_selection",selectedCount:selected.size}});redirect("/app/integrations?success=Meta%20Ads%20accounts%20saved.")}
