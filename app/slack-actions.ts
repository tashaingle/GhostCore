"use server";import{redirect}from"next/navigation";import{getActiveOrganisation}from"@/lib/organisations/active";import{requireOrganisationAdmin}from"@/lib/auth/organisation-admin";import type{Json}from"@/types/database";export async function saveSlackChannels(form:FormData){const ctx=await getActiveOrganisation();if(!ctx)redirect("/login");requireOrganisationAdmin(ctx.membership.role);const integrationId=String(form.get("integrationId")||""),selected=new Set(form.getAll("channel").map(String).slice(0,50)),{data:item}=await ctx.supabase.from("integrations").select("settings").eq("id",integrationId).eq("organisation_id",ctx.organisation.id).eq("provider","slack").maybeSingle();if(!item)redirect("/app/integrations?error=Slack%20integration%20not%20found.");const settings=item.settings as Record<string,Json>,before=Array.isArray(settings.channels)?settings.channels as Record<string,Json>[]:[],channels=before.map(c=>({...c,selected:selected.has(String(c.id))}));await ctx.supabase.from("integrations").update({settings:{...settings,channels,configurationStatus:selected.size?"ready":"property_required"}}).eq("id",integrationId).eq("organisation_id",ctx.organisation.id);await ctx.supabase.from("integration_logs").insert({organisation_id:ctx.organisation.id,integration_id:integrationId,provider:"slack",status:"finished",records_received:channels.length,events_imported:0,events_skipped:0,error_count:0,metadata:{operation:"channel_selection",selectedCount:selected.size,previousSelectedCount:before.filter(c=>c.selected).length}});redirect("/app/integrations?success=Slack%20channels%20saved.")}
