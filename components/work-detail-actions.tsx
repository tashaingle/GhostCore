import{commentAction,evidenceAction,workAction}from"@/app/work-actions";import type{OrganisationRole}from"@/lib/auth/permissions";import{hasPermission}from"@/lib/auth/permissions";
export function WorkDetailActions({kind,id,status,role,members}:{kind:"task"|"case";id:string;status:string;role:OrganisationRole;members:{user_id:string}[]}){const transitions=kind==="task"?status==="open"?["in_progress","blocked","waiting","cancelled"]:status==="in_progress"?["blocked","waiting","completed","cancelled"]:status==="blocked"||status==="waiting"?["open","in_progress","cancelled"]:["open"]:status==="open"?["investigating","waiting","resolved","cancelled"]:status==="investigating"?["waiting","resolved","cancelled"]:status==="waiting"?["investigating","resolved","cancelled"]:status==="resolved"?["closed","investigating"]:["investigating"];return <div className="space-y-4"><div className="card"><h3 className="font-semibold">Actions</h3>{hasPermission(role,"work.assign")&&<form action={workAction} className="mt-3 flex flex-wrap gap-2"><input type="hidden" name="kind" value={kind}/><input type="hidden" name="id" value={id}/><select className="field" name="assignedUserId" required><option value="">Select assignee</option>{members.map(x=><option key={x.user_id} value={x.user_id}>{x.user_id}</option>)}</select><button className="button button-secondary" name="action" value="assign">Assign</button><button className="button button-secondary" name="action" value="unassign">Unassign</button></form>}{hasPermission(role,"work.manage")&&<form action={workAction} className="mt-3 grid gap-2"><input type="hidden" name="kind" value={kind}/><input type="hidden" name="id" value={id}/><textarea className="field" name="reason" placeholder="Reason (required for block, wait, cancel or reopen)"/><textarea className="field" name="summary" placeholder={kind==="task"?"Completion summary":"Resolution summary"}/><input className="field" name="actualMinutes" type="number" min="0" placeholder="Actual minutes"/><div className="flex flex-wrap gap-2">{transitions.map(x=><button className={["cancelled","closed"].includes(x)?"button button-secondary":"button"} key={x} name="action" value={x}>{x.replaceAll("_"," ")}</button>)}</div></form>}</div>{hasPermission(role,"work.comment")&&<form action={commentAction} className="card space-y-2"><input type="hidden" name="kind" value={kind}/><input type="hidden" name="id" value={id}/><h3 className="font-semibold">Comment or internal note</h3><textarea className="field min-h-24 w-full" name="body" required maxLength={5000}/><select className="field w-full" name="commentType"><option value="comment">Comment</option>{hasPermission(role,"work.internal_notes")&&<option value="internal_note">Internal note</option>}</select><button className="button">Add activity</button></form>}{hasPermission(role,"work.manage")&&<form action={evidenceAction} className="card grid gap-2"><input type="hidden" name="kind" value={kind}/><input type="hidden" name="id" value={id}/><h3 className="font-semibold">Link structured evidence</h3><input className="field" name="label" required placeholder="Evidence label"/><input className="field" name="sourceType" required placeholder="notification, approval, event…"/><input className="field" name="sourceId" required placeholder="Source ID"/><input className="field" name="evidenceType" defaultValue="reference"/><textarea className="field" name="description" placeholder="Safe description"/><button className="button button-secondary">Link evidence</button></form>}</div>}
