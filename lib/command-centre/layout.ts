import{COMMAND_WIDGETS,type CommandWidgetId,type SavedCommandLayout}from"./types";
export const defaultCommandLayout:SavedCommandLayout={name:"Default",widgets:[...COMMAND_WIDGETS],hidden:[],collapsed:[],pinnedProviders:[],pinnedEventIds:[],dateRange:"7d"};
export function parseCommandLayout(input:unknown):SavedCommandLayout{if(!input||typeof input!=="object"||Array.isArray(input))return defaultCommandLayout;const value=input as Record<string,unknown>,valid=(x:unknown):x is CommandWidgetId=>typeof x==="string"&&COMMAND_WIDGETS.includes(x as CommandWidgetId),widgets=Array.isArray(value.widgets)?value.widgets.filter(valid):[],ordered=[...new Set([...widgets,...COMMAND_WIDGETS])],hidden=Array.isArray(value.hidden)?value.hidden.filter(valid):[],collapsed=Array.isArray(value.collapsed)?value.collapsed.filter(valid):[],range=["today","7d","30d"].includes(String(value.dateRange))?String(value.dateRange)as SavedCommandLayout["dateRange"]:"7d";return{name:String(value.name??"Default").slice(0,80),widgets:ordered,hidden,collapsed,pinnedProviders:Array.isArray(value.pinnedProviders)?value.pinnedProviders.filter(x=>typeof x==="string").slice(0,12):[],pinnedEventIds:Array.isArray(value.pinnedEventIds)?value.pinnedEventIds.filter(x=>typeof x==="string"&&/^[0-9a-f-]{36}$/i.test(x)).slice(0,25):[],dateRange:range,updatedAt:typeof value.updatedAt==="string"?value.updatedAt:undefined}}
export function reorderWidgets(order:Record<string,number>,hidden:string[],collapsed:string[],pinnedProviders:string[],pinnedEventIds:string[],dateRange:string):SavedCommandLayout{const widgets=[...COMMAND_WIDGETS].sort((a,b)=>(order[a]??99)-(order[b]??99));return parseCommandLayout({name:"Organisation command centre",widgets,hidden,collapsed,pinnedProviders,pinnedEventIds,dateRange,updatedAt:new Date().toISOString()})}
