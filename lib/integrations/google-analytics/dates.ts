export type ComparisonDates={currentStart:string;currentEnd:string;previousStart:string;previousEnd:string;comparisonType:"rolling_7_day"};
const shift=(date:string,days:number)=>{const value=new Date(`${date}T12:00:00Z`);value.setUTCDate(value.getUTCDate()+days);return value.toISOString().slice(0,10)};
export function dateInTimeZone(now:Date,timeZone:string){const parts=new Intl.DateTimeFormat("en-CA",{timeZone,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(now);const get=(type:string)=>parts.find(part=>part.type===type)?.value;return `${get("year")}-${get("month")}-${get("day")}`}
export function completeSevenDayRanges(now:Date,timeZone:string):ComparisonDates{const today=dateInTimeZone(now,timeZone);const currentEnd=shift(today,-1),currentStart=shift(currentEnd,-6),previousEnd=shift(currentStart,-1),previousStart=shift(previousEnd,-6);return{currentStart,currentEnd,previousStart,previousEnd,comparisonType:"rolling_7_day"}}
export function periodOccurrence(endDate:string,timeZone:string){const noon=new Date(`${endDate}T12:00:00Z`);const offsetName=new Intl.DateTimeFormat("en-US",{timeZone,timeZoneName:"longOffset"}).formatToParts(noon).find(part=>part.type==="timeZoneName")?.value.replace("GMT","")||"+00:00";return new Date(`${endDate}T23:59:59${offsetName||"+00:00"}`).toISOString()}
