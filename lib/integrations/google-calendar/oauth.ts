import "server-only";import {createHash,randomBytes,timingSafeEqual}from"node:crypto";export const CALENDAR_SCOPES=["openid","email","https://www.googleapis.com/auth/calendar.events.readonly","https://www.googleapis.com/auth/calendar.calendarlist.readonly"] as const;export const secret=()=>randomBytes(32).toString("base64url");export const challenge=(v:string)=>createHash("sha256").update(v).digest("base64url");export const stateMatches=(a?:string,b?:string|null)=>{if(!a||!b)return false;const x=Buffer.from(a),y=Buffer.from(b);return x.length===y.length&&timingSafeEqual(x,y)};const env=()=>{const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET,redirectUri=process.env.GOOGLE_CALENDAR_REDIRECT_URI??`${process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000"}/api/integrations/google-calendar/callback`;if(!clientId||!clientSecret)throw new Error("Google OAuth is not configured.");return{clientId,clientSecret,redirectUri}};export function authorisationUrl(state:string,verifier:string){const e=env(),q=new URLSearchParams({client_id:e.clientId,client_secret:"",redirect_uri:e.redirectUri,response_type:"code",scope:CALENDAR_SCOPES.join(" "),access_type:"offline",prompt:"consent",state,code_challenge:challenge(verifier),code_challenge_method:"S256"});q.delete("client_secret");return`https://accounts.google.com/o/oauth2/v2/auth?${q}`}export async function exchange(code:string,verifier:string,request:typeof fetch=fetch){const e=env(),r=await request("https://oauth2.googleapis.com/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({client_id:e.clientId,client_secret:e.clientSecret,redirect_uri:e.redirectUri,grant_type:"authorization_code",code,code_verifier:verifier})}),b=await r.json()as{access_token?:string;refresh_token?:string;expires_in?:number;scope?:string;error_description?:string};if(!r.ok||!b.access_token)throw new Error(b.error_description??"Calendar authorization failed.");if(!b.scope?.includes("calendar.events.readonly")||!b.scope.includes("calendar.calendarlist.readonly"))throw new Error("Required Calendar read-only permissions were not granted.");return b}
