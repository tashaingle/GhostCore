import type{IntegrationConnector,RawProviderRecord,TranslationContext}from"../connector";import{manualTranslator}from"../manual-translator";export class ManualConnector implements IntegrationConnector{readonly provider="manual";connect=async()=>({ok:true});disconnect=async()=>({ok:true});refresh=async()=>({ok:true});healthCheck=async()=> "healthy" as const;sync=async()=>({received:0,events:[],filtered:0});translate(record:RawProviderRecord,context:TranslationContext){return manualTranslator.translate(record,context)}}
