import"server-only";export const NOTION_API_VERSION="2026-03-11",NOTION_LIMITS={pages:100,rows:10000,retries:2,timeoutMs:15000,initialDays:90}as const;export function notionEnv(){const clientId=process.env.NOTION_CLIENT_ID,clientSecret=process.env.NOTION_CLIENT_SECRET,redirectUri=process.env.NOTION_REDIRECT_URI;if(!clientId||!clientSecret||!redirectUri)throw new Error("Notion OAuth configuration is incomplete.");return{clientId,clientSecret,redirectUri}}
