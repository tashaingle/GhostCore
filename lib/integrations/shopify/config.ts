import"server-only";export const SHOPIFY_API_VERSION="2026-07";export const SHOPIFY_SCOPES=["read_orders","read_products","read_inventory","read_discounts","read_locations"]as const;export const SHOPIFY_LIMITS={initialDays:60,pages:5,itemsPerPage:100,requests:20,runtimeMs:25_000,events:500}as const;export function shopifyEnv(){const clientId=process.env.SHOPIFY_CLIENT_ID,clientSecret=process.env.SHOPIFY_CLIENT_SECRET,redirectUri=process.env.SHOPIFY_REDIRECT_URI;if(!clientId||!clientSecret||!redirectUri)throw new Error("Shopify OAuth configuration is incomplete.");return{clientId,clientSecret,redirectUri}}
