import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/types/database";import{createEvent}from"@/lib/events/create-event";
export async function notificationEvent(client:SupabaseClient<Database>,userId:string,item:{id:string;organisation_id:string;rule_key:string;category:string;severity:string;status:string},change:string,revisionId:string,actorType:"user"|"system"|"service",actorUserId?:string|null){return createEvent(client,userId,{organisationId:item.organisation_id,source:"ghost",category:item.category==="security"?"security":"operations",eventType:`notification.${change}`,title:`Notification ${change}: ${item.rule_key}`,description:`Action Centre item ${item.id} changed to ${item.status}.`,severity:item.severity==="critical"?"critical":item.severity==="warning"?"warning":"info",occurredAt:new Date().toISOString(),externalId:`notification:${revisionId}`,metadata:{notificationId:item.id,ruleKey:item.rule_key,category:item.category,severity:item.severity,status:item.status,actorType,actorUserId:actorUserId??null,revisionId}})}
