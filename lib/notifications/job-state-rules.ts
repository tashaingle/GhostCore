import type{NotificationRuleDefinition,NotificationCandidate,NotificationRuleContext,NotificationSeverity}from"./types";
const clean=(x:string|null)=>String(x??"No safe error detail recorded.").replace(/bearer\s+\S+|token[=:]\s*\S+/gi,"[credential removed]").slice(0,500);
const make=(c:NotificationRuleContext,r:NotificationRuleDefinition,x:Omit<NotificationCandidate,"organisationId"|"ruleKey"|"ruleVersion"|"category"|"fingerprintParts">):NotificationCandidate=>({organisationId:c.organisationId,ruleKey:r.key,ruleVersion:r.version,category:r.category,fingerprintParts:[c.organisationId,r.key,String(r.version),x.sourceType,x.sourceId,x.condition],...x});
const latestRuns=async(c:NotificationRuleContext)=>{const{data:runs}=await c.client.from("background_job_runs").select("id,job_id,status,attempt,error,retry_at,completed_at,created_at").eq("organisation_id",c.organisationId).order("created_at",{ascending:false}).limit(c.limit*5),latest=new Map<string,NonNullable<typeof runs>[number]>();for(const run of runs??[])if(!latest.has(run.job_id))latest.set(run.job_id,run);return latest};
const failed:NotificationRuleDefinition={key:"background_job_failed",version:1,name:"Background job failed",description:"The latest durable run for a job finished unsuccessfully.",category:"background_job",defaultSeverity:"warning",async evaluate(c){const latest=await latestRuns(c),{data:jobs}=await c.client.from("background_jobs").select("id,job_key,consecutive_failures,last_success_at").eq("organisation_id",c.organisationId),threshold=Number(c.configuration.criticalFailureThreshold??process.env.NOTIFICATION_CRITICAL_FAILURE_THRESHOLD??3);return(jobs??[]).flatMap(job=>{const run=latest.get(job.id);if(!run||!["failed","timed_out"].includes(run.status)||job.job_key==="notification.generate")return[];const severity:NotificationSeverity=job.consecutive_failures>=threshold?"critical":"warning";return[make(c,failed,{severity,title:`${job.job_key} failed`,summary:clean(run.error),explanation:`The latest run finished as ${run.status}. Critical severity starts at ${threshold} consecutive failures.`,recommendedAction:"Review the run, correct the deterministic failure, and retry.",sourceType:"background_job",sourceId:job.id,condition:"failed",evidence:[{evidenceType:"job_run",sourceTable:"background_job_runs",sourceId:run.id,jobRunId:run.id,label:"Latest failed job run",description:`Attempt ${run.attempt}; ${clean(run.error)}`,observed:{status:run.status,attempt:run.attempt,consecutiveFailures:job.consecutive_failures,lastSuccessAt:job.last_success_at},expected:{status:"completed"},occurredAt:run.completed_at??run.created_at}]})]})}};
const retrying:NotificationRuleDefinition={key:"background_job_retrying",version:1,name:"Background job retrying",description:"The latest run for a job is waiting for deterministic retry.",category:"background_job",defaultSeverity:"warning",async evaluate(c){const latest=await latestRuns(c),{data:jobs}=await c.client.from("background_jobs").select("id,job_key").eq("organisation_id",c.organisationId);return(jobs??[]).flatMap(job=>{const run=latest.get(job.id);if(!run||run.status!=="retrying"||job.job_key==="notification.generate")return[];return[make(c,retrying,{severity:run.attempt>=3?"critical":"warning",title:`${job.job_key} is retrying`,summary:`Attempt ${run.attempt} is waiting for deterministic backoff.`,explanation:"The latest failure was classified as retryable.",recommendedAction:"Review the safe error and wait for or trigger the retry.",sourceType:"background_job",sourceId:job.id,condition:"retrying",evidence:[{evidenceType:"job_retry",sourceTable:"background_job_runs",sourceId:run.id,jobRunId:run.id,label:"Latest retry state",description:clean(run.error),observed:{attempt:run.attempt,retryAt:run.retry_at},occurredAt:run.created_at}]})]})}};
export const jobStateNotificationRules=[failed,retrying]as const;
